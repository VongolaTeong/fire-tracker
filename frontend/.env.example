# Base URL of the FIRE Tracker backend API. Copy this file to `.env.development` (local) or set
# the variable in your deploy environment. Defaults to http://localhost:8080 when unset.
VITE_API_BASE_URL=http://localhost:8080
